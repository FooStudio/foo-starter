import "styles/views/_{{#camelize}}{{name}}{{/camelize}}"
import React from "react"
import {render} from "react-dom"
import View from "foo/core/react/View"

export default class {{#camelize}}{{name}}{{/camelize}} extends View {

    onRender(){

    }

    componentWillEnter(callback) {
        callback();
    }

    componentWillLeave(callback) {
        callback();
    }

    render() {
        return (<div className="{{#camelize}}{{name}}{{/camelize}}"></div>)
    }
}
